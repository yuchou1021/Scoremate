fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
httpx>=0.27
# 仅测试需要（新版 starlette TestClient）：pip install httpx2
# music21 为可选增强（更丰富的和声分析，v0.2 起使用）；
# v0.1 的调性/音域/难度分析为纯 Python 实现，未安装也能跑。
music21>=9.1
